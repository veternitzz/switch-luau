[package]
name = "veternitzz/switch"
version = "2.1.2"
registry = "https://github.com/UpliftGames/wally-index"
license = "MIT"
description = "A switch implementation in pure luau."
realm = "shared"
exclude = ["**"]
include = ["src/**", "wally.toml", "LICENSE"]

[dependencies]
